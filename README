# Dashboard construido com React + Redux

Executando o projeto
--------------------

Clone o projeto para seu computador:
`git clone 

Entre no repositorio clonado:
`cd dashboard`

Instale as dependências para o projeto:
`npm install`

Inicie o projeto:
`npm start` -> para gerar codigo de desenvolvimento
ou
'npm run deploy' -> para gerar codigo de producao

projeto estará rodando na url:
`localhost:8080`